version: '3.8'

services:
  frontend:
    build: .
    container_name: vite-app
    ports:
      - "8080:4173"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
